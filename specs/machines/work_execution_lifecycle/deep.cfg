SPECIFICATION Spec
CONSTANTS
  BooleanValues = {TRUE, FALSE}
  NatValues = {0, 1, 2}
  StringValues = {"alpha", "beta"}
  WorkExecutionEvidenceKindValues = {"Completed", "Failed", "Canceled", "LaunchFailed", "RunLost"}
INVARIANTS
  identified_after_bind
  evidence_projection_is_typed
CONSTRAINTS
  DeepStateConstraint
